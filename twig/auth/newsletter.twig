{% include '/general/header.twig' %}
<section>
  <div class="container d-flex flex-column">
    <div class="row align-items-center justify-content-center min-vh-100">
      <div class="col-md-8 col-lg-5 py-6">
        <h1
          class="text-center">{{ 'site.unsubscribeNewsletter' | language }}</h1>

        {% if newsletter['success'] %}
        <h3 class="text-center">{{ 'site.unsubscribeNewsletter.done' | language }}
          {% else %}
          <h3 class="text-center">{{ 'site.unsubscribeNewsletter.error' | language }}
            {% endif %}

      </div>
    </div>
  </div>
</section>
{% include '/general/footer.twig' %}